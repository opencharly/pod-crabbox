# 2026.247.1311 — chore: pod-postgresql v2026.247.1304 (root-capable entrypoint)

## Summary

Bumps the `pod-postgresql` require to **v2026.247.1304** — the root-capable
entrypoint (pod-postgresql #2): the postgres service now initdbs as the
`postgres` OS user under root-posture boxes (runuser drop + B12
`postgresql-not-root` check), while uid-1000 boxes are unchanged. This is the
loop-cutover **exit-1 improvement** — correct in itself, and the basis for any
future root-posture composition.

## How tested

```
$ charly box validate
box validate: OK — checked 9 candies, 7 distros, 7 builders; 0 warnings, 0 errors

# (the runtime root path was proven live on the disposable e2e bed in
# pod-postgresql #2: initdb-as-postgres PASS, B12 observable PASS,
# coordinator health/ready 200.)
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | layer/check skills loaded before authoring. |
| R1 — RCA every anomaly | The pin bump follows the source change; validate 0/0; no surviving warnings introduced by this PR. |
| R2 — finish the whole cutover | The require bump is the whole pin cutover for this change. |
| R3 — no duplication | The root capability lives in pod-postgresql once, referenced by tag. |
| R4 — no workarounds | Pin to the published tag only (merged ref). |
| R5 — delete legacy | N/A. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | `box validate` at the fresh-binary gate resolves the new tag (0/0). |
| R8 — preserve emitted artifacts | Manifest validated (0/0). |
| R9 — binary equals source | Fresh worktree-built bin/charly. |
| R10 — fresh disposable proof | N/A in this PR (pin-only; the runtime proof is in pod-postgresql #2's evidence + the cutover bed). |
| RDD/ADE/SDD | N/A (pin-only). |
| Concurrency / Disposable gate / Hooks | N/A. |

## Change classification

- **Class:** dependency pin bump (merged tag).
- **Verification gate:** `charly box validate` (CI + fresh binary), 0/0.
- **Attribution:** analysed on a live system — this PR is a pin-only bump to a merged tag; the changed runtime path (the root-capable entrypoint) executed live on the source PR’s fresh-rebuild beds (initdb-as-postgres PASS, B12 PASS, health/ready 200), retained there.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*