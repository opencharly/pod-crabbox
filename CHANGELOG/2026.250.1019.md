# 2026.250.1019 — feat: coordinator tag v0.49.0 -> v0.51.0 (CLI pairing)

## Summary

Bumps the coordinator pin to the tag that pairs with the **v0.51.0 CLI**: `CRABBOX_COORDINATOR_TAG v0.49.0 -> v0.51.0` (the upstream release note requires the coordinator to record `cleanupCompletedAt`, #1901 — CLI and coordinator move in lockstep). Candy `version:` → `2026.250.0200`; description notes the pairing requirement.

## How tested

Fresh-rebuild bed run on the `disposable: true` target `check-crabbox-e2e-pod` (repo overrides feed these PR branches into the build):

```
EVAL BED: check-crabbox-e2e-pod — exit 0 (13/13 steps)
  [image-build] ok 1m0.6s   [check-image] ok 26.8s
  [deploy-add] ok 4.3s      [config] ok 6.0s
  [start] ok 3.7s           [bring-up-members] ok
  [check-live] ok 54.2s     (126 live steps: 106 passed, 0 failed, 20 skipped)
  [feature-run] ok 18.4s
  [update] ok 18.3s         (R10 fresh-rebuild gate — coordinator redeployed at v0.51.0)
  [check-live-rebuild] ok 48.0s   [feature-run-rebuild] ok
  [cleanup] ok              [cleanup-members] ok
PASS [cbx-e2e-health]   status=200   (coordinator /v1/health)
PASS [cbx-e2e-ready]    status=200   (coordinator /v1/ready)
PASS [cbx-e2e-login]    exit=0       (shared-token into the v0.51.0 coordinator)
PASS [cbx-e2e-doctor]   ok  config=/home/user/.config/crabbox/config.yaml; provider=local-container
PASS [cbx-e2e-usage]    usage month=2026-09 scope=user user=crabbox@local
PASS [cbx-e2e-leases]   exit=0
PASS [cbx-e2e-loop]     exit=0   <- THE FULL LOCAL LOOP (crabbox run --provider local-container, stdout crabbox-local-ok) — v0.51.0 CLI + v0.51.0 coordinator pairing, pre- AND post-update
```

The `[update]` fresh-rebuild leg redeployed the rebuilt image (coordinator v0.51.0) and the post-update check-live re-verified everything green.

## Rulebook compliance
- R1: the pairing requirement comes from the upstream release note, documented rather than ignored.
- R7: e2e bed output pasted above (exit 0, 13/13, coordinator health/ready/login/usage/leases/loop all green).
- R10: fresh-rebuild disposable proof incl. the update leg.
- R2/R3/R4/R5/R6/R8/R9: N/A or satisfied — pin alignment; branch-per-change PR.

## Change classification

- **Class:** candy pin alignment (contract consumer). **Verification gate:** fresh-rebuild disposable e2e bed (exit 0, 13/13). **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*